/*
Faltou incluir as bibliotecas <stdio.h> e <stdlib.h>
Não declarou a variável main como (int) e não colocou o return 0 no final
Na linha das variáveis está com ";" em vez de "," entre as variáveis e a linha do código deve terminar com ";" e não ":"

*/
#include <stdio.h>
int main(){
    int a=1, b=2, c=3;
    printf("Os números são: %d, %d e %d\n", a, b, c);
    system("pause");

    return 0;
}
